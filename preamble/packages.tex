% Encodage et langue
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french,english]{babel}

% Polices professionnelles
\usepackage{lmodern}
\usepackage[scaled=0.92]{helvet}

% Couleurs et graphismes
\usepackage[dvipsnames,svgnames,x11names]{xcolor}
\usepackage{tikz}
\usetikzlibrary{calc}

% Carnet de chants avec accords
\usepackage[chorded]{songs}

% Mise en page A5
\usepackage[a5paper, landscape, inner=30mm, outer=10mm, top=18mm, bottom=14mm,
            headheight=16pt, headsep=8pt, footskip=10mm]{geometry}

% En-têtes et pieds de page
\usepackage{fancyhdr}

% Images (nœuds scouts)
\usepackage{graphicx}

% Liens cliquables dans le PDF
\usepackage[hidelinks]{hyperref}

% Espace minimum avant un chant (évite les titres orphelins)
\usepackage{needspace}

% Couleur de fond des pages
\usepackage{pagecolor}
